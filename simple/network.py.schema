info:
    version: 5
    title: Network and subnet for Neo4j deployment

properties:
    region:
        type: string
        default: us-east1

outputs:
    networkRef:
        type: string
    subnetRef:
        type: string
    subnetCidr:
        type: string
