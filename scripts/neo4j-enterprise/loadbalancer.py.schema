info:
    version: 5
    title: External Load Balancer service for Neo4j Browser and Bolt

required:
    - instance_group
properties:
    region:
        type: string
        default: us-east1
    instance_group:
        type: string

outputs:
    ip:
        type: string
